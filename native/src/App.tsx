import React from 'react';
import { SafeAreaView } from 'react-native';

import Home from '@common/pages';

const App = () => {
  return (
    <SafeAreaView>
      <Home />
    </SafeAreaView>
  );
};

export default App;
